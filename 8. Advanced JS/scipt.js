// Ternary Operator

function isUserValid(bool){
	return bool;
}

var ans = isUserValid(true) ? "You may enter":"Acess Denied";

var automatedAns = "Your account number is "+ (isUserValid(false) ? "1234" : "not available");


// Switch Statment

